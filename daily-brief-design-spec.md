# Daily Brief 프로젝트 설계 명세서

> 개인용 일일 지식 브리핑 자동화 시스템
> 이 문서는 Claude Code로 실제 개발을 진행하기 위한 설계 참고 문서입니다.

---

## 1. 프로젝트 목표

관심 분야(게임/IT/AI 중심 + 한국 주요뉴스)의 뉴스를 매일 자동으로 수집·요약하고,
상식과 영어 학습 콘텐츠까지 곁들여 **매일 아침 습관처럼 확인하는 개인용 지식 브리핑**을 만든다.

- 대상 사용자: 1인 전용 (계정/인증 시스템 불필요)
- 완전 자동화: 사람 개입 없이 매일 콘텐츠 생성 및 게시
- 열람 방식: 알림 없이, 사용자가 스스로 웹페이지를 열어서 확인

---

## 2. 콘텐츠 구성

하루 분량 기준 약 1시간 분량. 아래 5개 섹션으로 구성.

### 2.1 게임/IT/AI 뉴스 (핵심)
- 기사별 3줄 요약
- 💡 토막지식 1개 (기사에 나온 용어/기업/기술 배경 설명)

### 2.2 한국 주요뉴스 (서브, 상식 차원)
- 헤드라인 + 1-2줄 간단 요약

### 2.3 오늘의 상식
- 3-4개, 카테고리(역사/과학/경제/문화 등) 로테이션 없이 그때그때 다양하게 구성

### 2.4 영어 학습
- 단어/표현 5개 내외 (뜻 + 예문), 가능하면 당일 수집 기사 원문에서 추출
- 짧은 지문 1개 + 해석 연습

### 2.5 복습
- 어제 또는 최근 며칠 치 상식/영어 표현 중 2-3개를 가볍게 재소환
- 퀴즈/상호작용 없음, 단순 재노출

**명시적으로 제외한 것**: 퀴즈, 개인화/난이도 조정 로직, RAG/벡터DB (1인 사용 규모에는 오버스펙으로 판단)

---

## 3. 뉴스 소스 목록

| 카테고리 | 소스 |
|---|---|
| 게임/IT/AI (해외) | TechCrunch, The Verge, Ars Technica, VentureBeat AI, IGN |
| IT (국내) | ZDNet Korea (zdnet.co.kr) |
| 게임 (국내) | 디스이즈게임, 인벤 |
| 한국 주요뉴스 | 네이버뉴스 오픈API (Client ID/Secret 발급 필요) |

수집은 RSS 우선, 네이버뉴스는 오픈API 사용.

---

## 4. 시스템 아키텍처

### 4.1 개발 환경 vs 실행 환경 분리

- **개발**: 로컬 노트북 + Claude Code (Pro 플랜) — 스크립트 작성 및 테스트
- **실행(운영)**: GitHub Actions — 노트북과 완전히 무관하게 매일 자동 실행
- **AI 처리**: Claude API 별도 키 필요 (Claude Code Pro 플랜과는 별개, 사용량 기반 과금)
- **호스팅**: GitHub Pages (무료 정적 사이트)

### 4.2 전체 파이프라인

```
GitHub Actions (매일 07:00 KST 자동 트리거)
  ↓
① 뉴스 수집 (RSS + 네이버뉴스 오픈API)
  ↓
② Claude API 호출
   - 입력: 오늘 수집된 기사 원문 + 최근 데이터(복습용 컨텍스트)
   - 출력: 구조화된 JSON (아래 4.4 참고)
  ↓
③ JSON 원본 저장 (data/YYYY-MM-DD.json, 무제한 누적 보관)
  ↓
④ HTML 생성 (JSON → 오늘자 페이지 + 아카이브 갱신)
  ↓
⑤ GitHub Pages 배포 (자동 커밋 & push)
```

### 4.3 저장소 구조

```
daily-brief/
├── .github/
│   └── workflows/
│       └── daily.yml          # 매일 07:00 KST 자동 실행 (cron: '0 22 * * *' UTC)
├── scripts/
│   ├── fetch_news.py            # RSS + 네이버뉴스 API 수집
│   ├── generate_content.py      # Claude API 호출, JSON 생성
│   └── build_html.py            # JSON → HTML 변환 (오늘자 + 아카이브 갱신)
├── data/
│   └── YYYY-MM-DD.json          # 날짜별 원본 데이터 (무제한 누적)
├── docs/                         # GitHub Pages 배포 대상 폴더
│   ├── index.html                # 오늘 브리핑
│   ├── archive.html              # 검색 가능한 과거 목록
│   └── YYYY-MM-DD.html            # 날짜별 페이지 (누적)
├── requirements.txt
└── README.md
```

API 키(ANTHROPIC_API_KEY, 네이버 API 키)는 GitHub 저장소의 **Secrets**에 등록하여 코드 노출 방지.

### 4.4 Claude API 출력 JSON 스키마 (초안)

```json
{
  "date": "2026-07-03",
  "news_global": [
    {"title": "", "summary_3lines": "", "trivia": ""}
  ],
  "news_korea_it": [
    {"title": "", "summary_3lines": "", "trivia": ""}
  ],
  "news_korea_general": [
    {"title": "", "summary_short": ""}
  ],
  "common_sense": [
    {"topic": "", "category": "", "content": ""}
  ],
  "english": {
    "expressions": [
      {"term": "", "meaning": "", "example": ""}
    ],
    "passage": {"text": "", "translation_hint": ""}
  },
  "review": [
    {"from_date": "", "content": ""}
  ]
}
```

프롬프트 설계 시 유의사항:
- 출력을 JSON only로 강제 (파싱 편의를 위해)
- "카테고리 로테이션 없이 다양하게 섞어서" 상식 생성하도록 지시
- 복습 항목은 `data/` 폴더 최근 1-2개 파일을 컨텍스트로 첨부하여 생성

---

## 5. 아카이브 / 과거 조회 설계

- **보관 정책**: 삭제 없이 전체 무제한 보관
- **재조회 가능성**: 높음 → 단순 날짜 목록이 아니라 검색 가능한 형태로 설계
- `archive.html`에 검색창 + 카테고리 필터(뉴스/상식/영어) 제공
- 클라이언트 사이드 JS로 JSON 기반 키워드 검색 구현 (별도 서버/DB 불필요)
- 데이터 용량은 텍스트 기반이라 장기간 누적되어도 GitHub 무료 한도 내 충분

---

## 6. UI/디자인 방향

- **다크/라이트 모드 토글** 가능하게 구현
- **좌우 스와이프**로 섹션 전환 (뉴스 → 한국뉴스 → 상식 → 영어 → 복습)
- 각 섹션 내부는 **아코디언(접기/펼치기)** 구조 — 제목만 먼저 보이고 탭하면 펼쳐짐
- 하단에 섹션 위치 인디케이터 (점 형태)
- "지난 브리핑 보기" 링크로 아카이브 페이지 이동
- 알림 기능 없음 — 사용자가 매일 스스로 홈 화면 아이콘(바로가기)을 탭해서 확인하는 방식

---

## 7. 비용

| 항목 | 비용 |
|---|---|
| GitHub Actions | 무료 (하루 1회, 몇 분짜리 작업) |
| GitHub Pages | 무료 |
| Claude API | 사용량 기반, 일 1회 콘텐츠 생성 규모면 소액 |
| 네이버 오픈API | 무료 (호출량 제한 있음, 확인 필요) |

---

## 8. 개발 순서 제안

1. Git 저장소 초기화 + 폴더 구조 셋업
2. `fetch_news.py`: RSS/네이버API 수집 스크립트 작성 및 테스트
3. `generate_content.py`: Claude API 프롬프트 설계 및 JSON 생성 테스트
4. `build_html.py`: JSON → HTML 템플릿 렌더링 (다크모드, 스와이프, 아코디언 포함)
5. 로컬에서 전체 파이프라인 수동 실행 테스트
6. GitHub Actions workflow(`daily.yml`) 작성 및 Secrets 등록
7. GitHub Pages 활성화 및 배포 확인
8. 며칠간 운영하며 프롬프트/포맷 다듬기
9. (선택) 아카이브 검색 기능 고도화

---

## 9. 향후 확장 옵션 (지금은 보류)

- RAG 기반 의미 검색: 데이터가 충분히 쌓이면 고려 (현재는 단순 키워드 검색으로 충분)
- 오디오(TTS) 버전: 이동 중 청취용
- 개인화/난이도 조정: 데이터 축적 후 필요시 추가
- 텔레그램/이메일 등 알림 채널: 현재는 불필요하다고 판단, 필요시 발송 스크립트만 추가하면 확장 가능한 구조
